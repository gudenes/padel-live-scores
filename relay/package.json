{
  "name": "padel-relay",
  "version": "1.0.0",
  "description": "Persistent Pusher relay for PadelNacho — writes live match events to Supabase",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "express": "^4.18.2",
    "pusher-js": "^8.4.0-rc2"
  }
}
